//

#include "AI/NPCs/MythicNPCAIController.h"

// Intentionally empty: AMythicNPCAIController is a non-abstract concretization of AMythicAIController and adds no
// behavior of its own — it exists only so the abstract base controller can be auto-possessed by code-spawned NPCs.
// See the header.
